# Generated by Claude Fable 5 (Anthropic) — Grafana datasource for the fishiris container.
# uid matches the host Grafana's PostgreSQL datasource so the exported T0 panels resolve.
apiVersion: 1
datasources:
  - name: grafana-postgresql-datasource
    type: grafana-postgresql-datasource
    uid: ffpcavwwh7gg0e
    access: proxy
    url: localhost:5432
    user: fish
    isDefault: true
    jsonData:
      database: fish
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
    secureJsonData:
      password: fish
