# Overhead ölçümleri — veri envanteri (2026-09-01)
_Generated by Claude Fable 5 (Anthropic). Toplanan her şey + üretilebilecek tablolar._

## A. Ham veri (disk)

### Isaac kampanyası — ~/fish_traces/overhead_isaac/ (~89 GB)
- **13 bench TAM**: apriltag, bi3d, bi3d_freespace, dnn_image_encoder, dope,
  ess (wip2 telafisiyle), image_proc, nvblox, segformer (lttng taze üçlü),
  stereo_image_proc, tensor_rt, unet, visual_slam.
- Her bench: warmup + base×3 + lttng×3 + nsys×3. Koşu başına:
  `r2b-log-*.json` (ros2_benchmark'ın KENDİ raporu; 134 adet), `cpu.log`
  (container jiffy örnekleri; 172), `launch.log`, t_start/t_end.
- Traced koşularda tam FISH session (73 adet): LTTng CTF + nsys .sqlite/.rep +
  snapshot (env, ps_tree, params, topic_hz).
- `runs.csv`: 180 satırlık koşu kaydı (bench, mod, rep, rc, rapor-var-mı, epoch).
- Arşiv: `_archive/round1/` (bi3d, segformer, visual_slam — kullanıcı-yükü
  şüphesiyle; round-2 gece koşusu Fatih tetikleyecek), `_archive/ess_cudnn_fail/`,
  `_archive/segformer_lttng_old/`. `host_load_round2.log` (kısmi yük kaydı).
- **Veri üretemeyenler** (base dahil aynı hatayla düşüyor → FISH-bağımsız,
  rc'lerle belgelendi): triton+detectnet (Triton tensorrt backend image'da yok),
  rtdetr (prepare_buffer unsuccess), nitros_bridge (monitor'a mesaj yok).

### Autoware üç-mod — ~/fish_traces/overhead_aw_20260831_133353/ (68 MB)
- baseline/lttng/nsys ×3 = 9 koşu; her birinde `diag.log` (~7 MB — Autoware'in
  KENDİ ölçümleri: topic_state_monitor measured_rate, concat/centerpoint
  processing_time, pipeline_latency_monitor Total Latency), `cpu.log`, launch.log.
- 6 FISH session (lttng+nsys modları): toplam 30.5M event, 0 discard.

### Autoware pilot çiftleri — overhead_20260830_222059 / _223727
- traced(tam FISH) vs baseline; _223727 = temiz CPU verisi + hz problarıyla
  hz↔measured_rate çapraz doğrulaması (hz probları sonra protokolden çıkarıldı).

### PG'de incelemeye hazır
- unet nsys_1 (fish_20260830_224846) + Autoware r25 (fish_20260817_134427)
  tam ingest: tüm viz + gpu_dags (typed DAG'ler) üzerinde çalışıyor.

## B. Üretilmiş tablolar/özetler
- `notes/overhead_isaac_summary.md` — bench × mod: fps, jitter, CPU (±std, Δ%).
- `notes/overhead_autoware_modes.md` — 3-mod metrik tablosu (CPU, rate'ler,
  ihlal=0, concat/centerpoint proc, Total Latency; ±std, Δ%).
- `notes/overhead_autoware.txt` — pilot çift protokol + tablo.
- `overhead_isaac/summary.csv` — long-form (bench, mod, n, metrik mean/std).
- Trace sağlığı: Isaac 90 traced koşu / 60.1M event / 3 178 discard (%0.005,
  yalnız dnn_image_encoder-nsys ×3 + bi3d_freespace-nsys ×1); AW 30.5M / 0.
- Karar/olay kayıtları: `overhead_isaac_campaign.txt`,
  `cuda_event_trace_tradeoff.txt` (Isaac nsys modu bayraksız).

## C. Üretilebilecek tablolar/figürler (sıralı önerim)
1. **Overhead ∝ event-hacmi regresyonu** — 13 bench: fps düşüşü (lttng, nsys)
   vs event/s. WiP ana figürü adayı; veri hazır, yarım saatlik iş.
2. **Callback-bazlı exec-time lttng↔nsys eşleştirmesi** — aynı callback'in iki
   moddaki süre dağılımı, GPU'lu/CPU'lu ayrımıyla (Fatih'in önerisi). Traced
   session'lardan; en derin analiz, ~yarım gün.
3. **Kalibrasyon katsayısı c** (t_gerçek ≈ t_traced − n_event·c) — Autoware'de
   baseline self-measured proc time'lar ↔ traced değerlerden; "realistic exec
   time sunumu" için tek katsayı + doğrulama tablosu.
4. **Güven-aralıklı bar chart** — bench × mod fps (3 rep ±); WiP figürü.
5. **nsys'in ek maliyeti izole** — lttng vs nsys farkı ayrı tablo (Isaac'ta
   büyük, AW'de ~0 kontrastı; anlatı figürü).
6. **Fonksiyon-bütünlüğü tablosu** — AW: ihlal 0/0/0 + rate Δ; Isaac: rapor
   üretimi 117/117 + NUM_MISSED_FRAMES karşılaştırması.
7. **Jitter analizi** — r2b mean/max/std jitter mod karşılaştırması (tablo var,
   CDF/boxplot figürü çıkarılabilir).
8. **CPU zaman serileri** — cpu.log'lardan mod-overlay figür (bench başına).
9. **AW bileşen-bazlı processing-time tablosu** — diag'dan per-component
   3-mod (concat/centerpoint şu an var; tüm bileşenlere genişletilebilir).
10. **Reproducibility** — round-2 gece koşusu sonrası round1↔round2 base
    karşılaştırması + host-load korelasyonu (bekleyen: Fatih "koştur" diyecek).

## Bekleyenler
- Round-2 (bi3d, segformer, visual_slam) gece boş makinede — hazır script:
  `overhead_isaac/night_driver.sh` (sleep'i kaldırıp elle başlatılacak).
- (Opsiyonel) Autoware 3-mod gece tekrarı — gündüz koşmuştu; Δ'lar güvenilir
  ama mutlak değerler için temiz tekrar önerilir.
