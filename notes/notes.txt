FISH Development Notes — chronological journal
================================================


2026-04-19 (late evening) — oort/oore vertex types for non-executor GPU threads
--------------------------------------------------------------------------------

Investigating yolo_py in the AAS trace to start the GPU-DAG pipeline,
discovered it does not use any ROS 2 callback for inference:

  pid 1252 main thread:      1,029,170 cuda_runtime events, 0 callback_start
  pid 1252 ros_spin_thread:       0 cuda_runtime events, 59,407 callback_start
                                     (all TimeSource.clock_callback — housekeeping)

Source inspection confirms: yolo_node.py's run_inference_loop() runs on
the main thread with a plain while-loop over GStreamer frames + ONNX
Runtime (CUDA). `ros_spin_thread` runs rclpy.spin_once only to keep
sim-time current. Publishing is done via rclpy.Publisher (thread-safe,
no executor required).

Implication for the FISH task model: the GPU work has no F vertex to
attach to, because no callback fires for it. Instead of forcing a
callback-anchored model, we add two new attribute values:

  E.etype = "oore"  (out-of-ROS entity) placeholder — no real ROS
                    subscription/service/timer; exists so the oort
                    has a proper level-2 parent and the FISH
                    hierarchy invariant holds.
  F.ptype = "oort"  (out-of-ROS thread) the non-executor thread that
                    does the real work. gpu_node=True when it issues
                    CUDA calls.

New phase `detect_oort_threads` in model_improved.py, hooked after
attach_callback_groups. Algorithm:

  For each GPU-classified pid (nsys_profiled_pids):
    cuda_tids  = SELECT DISTINCT tid FROM fish.cuda_runtime
                 WHERE session=... AND container=...
    cb_tids    = set of vtids firing callback_start for pid in ros2_trace
    oort_tids  = cuda_tids - cb_tids       # CUDA active, no callbacks
    for each oort tid: create oore E (level 2) + oort F (level 3)
                       under the process's N vertex.

Verified on fish_compose_20260419_161633 aircraft scope:
  1 oort detected (tid=1252, yolo main thread)
  Composed graph: 131 vertices, 180 edges (was 129/178)
  JSON export preserves ptype/etype/thread_tid/gpu_node attributes

Additional findings & follow-ups captured for the paper:

  notes/EXPECTED_OBSERVED.txt §8 — full story of the non-executor
    GPU-thread pattern with measured numbers and source-code
    attribution.
  notes/EXPECTED_OBSERVED.txt §9 — nsys only records CUDA API call
    stacks for ~0.4 % of runtime rows (sync-class only). User-code
    attribution (onnxruntime → conv_N → ...) requires enabling
    --cudabacktrace=true on the nsys command line + extending
    ingest.py to pull CUDA_CALLCHAINS + StringIds. Tracked as
    immediate_work.txt item 0a (urgent).
  notes/known_bugs.txt BUG-003 (GPU↔ROS wall-clock offset) confirmed
    still fixed — measured alignment in current session:
      cuda_runtime first event:  2026-04-19 14:17:17.922 UTC
      callback_start first event: 2026-04-19 14:17:19    UTC (+2s boot lag)
    Period detection and cross-store correlation can use naive time
    comparison with no offset adjustment.

Next:
  - Configure FISH GPU daemon to pass --cudabacktrace=true (0a).
  - Extend ingest.py to pull CUDA_CALLCHAINS (0a part B).
  - postprocess/gpu_dag.py — period detection on CUDA activity of
    each oort thread, build Typed DAG (CPU/SM/CE nodes per PDF §4.5
    + §4.7 algorithm), collect unique DAG shapes, attach to F.A_v
    via a separate graph_store collection `gpu_dags` scoped the same
    way as graph_nodes/_edges.


2026-04-19 (evening) — session-per-DB refactor
----------------------------------------------

Followed up on the morning's rclpy work. The MongoDB DB layout was
broken: raw-trace DBs were named after the container role only
(aircraft / ground / simulation), so a second compose run clobbered
the previous mission's raw data. Graphs lived in a separate `fish_meta`
DB, not alongside their raw trace. Fixing this is a prerequisite for
the GPU/roofline work coming next.

New layout (see notes/db_structure.txt):

  One MongoDB database per session, named after the session directory:
    fish_compose_YYYYMMDD_HHMMSS  (multi-container)
    fish_YYYYMMDD_HHMMSS          (standalone)
  Collections inside:
    ros2_trace_<role>, snapshot_<role>, ... for per-container raw data
    graph_meta / graph_nodes / graph_edges / graph_mutations with
    `scope` field (role name for per-container, "__composed__" for
    the composed graph).

  One shared InfluxDB database: `fish` (one per installation, never
  per-session — InfluxDB 3 Core has a 5-DB limit). Points carry
  `session` and `container` tags.

Changes:
  postprocess/db_scope.py          NEW. ScopedMongo proxy wraps a
                                   pymongo Database, auto-suffixing
                                   scoped collection names with _<role>.
                                   Lets model_improved.py's bare
                                   mongo["ros2_trace"] calls keep
                                   working.
  postprocess/graph_store.py       API: (db_name, scope) pair replaces
                                   session_name. fish_meta DB removed.
                                   Collections: graph_meta / _nodes /
                                   _edges / _mutations inside each
                                   session DB. New list_sessions()
                                   lists all fish_* DBs. CLI grew a
                                   drop subcommand for full-session
                                   delete.
  postprocess/ingest.py            ingest_session(session_dir, *,
                                   role=None, db_name=None, ...). nsys
                                   → fixed "fish" InfluxDB DB with
                                   session+container tags on every
                                   point.
  postprocess/model_improved.py    connect_mongo returns ScopedMongo;
                                   connect_influx points at "fish".
                                   CLI: --role / --scope replace the
                                   old --container-role flag.
  postprocess/fish_compose.py      All three containers share one
                                   session DB. Per-container graphs
                                   saved under scope=<role>, composed
                                   under scope="__composed__".
  notes/db_structure.txt           NEW. Design doc — DB/collection
                                   convention, scope values,
                                   ScopedMongo rationale, migration
                                   note.

Legacy cleanup:
  Dropped intermediate `aircraft` / `ground` / `simulation` MongoDB
  DBs (they were the broken rotating state that's going away).
  Dropped `fish_meta` entries matching fish_compose_20260419*.
  Dropped all 5 legacy fish_YYYYMMDD_* InfluxDB databases (user
  confirmed they're not referenced in any empirical notes).
  Kept the MongoDB counterparts of those legacy sessions — notes
  under notes/ still cite them.

End-to-end verified on fish_compose_20260419_161633:
  Single MongoDB DB `fish_compose_20260419_161633` with
    ros2_trace_aircraft / ros2_trace_ground / ros2_trace_simulation
    plus per-container snapshot/node_info/topic_info/... and
    graph_meta/_nodes/_edges/_mutations scoped.
  Single InfluxDB DB `fish` with 5 measurements, points tagged
    session=fish_compose_20260419_161633 container=aircraft.
  Composed graph preserves all scheduler attributes from the morning
    (9/9 EX with executor_type, 37+ E with callback_group, 14 MX +
    23 Reentrant). yolo_py (rclpy) still populated.

Next: this was the last plumbing change before GPU/roofline work.
notes/immediate_work.txt #9 (per-callback roofline) is now
unblocked — GPU data sits in a queryable shared DB with the tags
we need to correlate against MongoDB graph_store scope + session.


2026-04-19
----------

rclpy scheduler tracepoints — parity with the C++ side.

1. Patches
   - fish_rclpy_trace.py ctypes bridge: 4 new wrappers
     (trace_executor_init / trace_cbgroup_init / trace_cbgroup_add /
     trace_executor_add_cbgroup). entity_addr uses
     `entity.handle.pointer` (rcl handle) so events join with existing
     rcl_*_init tracepoints — same schema as C++ side.
   - rclpy/executors.py: fire trace_executor_init at
     SingleThreadedExecutor and MultiThreadedExecutor __init__
     (after num_threads is resolved for MT). Fire
     trace_executor_add_cbgroup at the end of Executor.add_node for
     the node's default callback group.
   - rclpy/callback_groups.py: fire trace_cbgroup_init in
     CallbackGroup.__init__ (type inferred from subclass name — MX
     or Reentrant). Fire trace_cbgroup_add in CallbackGroup.add_entity.
   - install_fish_tracepoints extended with all of the above; Python
     anchors verified against real rclpy source + dry-run compiled
     before deploy.

2. First deploy — LTTng buffer overflow
   fish_compose_20260419_154439 (first rclpy run):
     aircraft fish_executor_add_cbgroup: 103,466 events
     aircraft fish_publish_link:              1 event
   fish_publish_link dropped from 88 (C++-only baseline) to 1 because
   the over-firing add_cbgroup events displaced it in the trace
   buffer. yolo_py alone produced ~25,000 add_cbgroup events from
   rclpy.spin_once's add_node/remove_node cycle.

3. Root cause
   rclcpp's Executor::add_callback_group_to_map is one-shot by
   construction (atomic exchange on an `associated_with_executor_`
   flag). rclpy's Executor.add_node is NOT one-shot — it returns
   True on every call where the node is not currently in the set.
   rclpy.spin_once(node) cycles add_node → spin → remove_node every
   iteration, so each iteration looks like a fresh add.

   The rclpy executor itself is NOT recreated each cycle, even
   though spin_once is a standalone function rather than a method.
   `get_global_executor()` returns a module-scoped singleton; each
   Python process has one long-lived rclpy executor. Confirmed by
   the trace: yolo_py (PID 1252) fired fish_executor_init exactly
   once.

4. Fix — bridge-side dedup
   fish_rclpy_trace._SEEN_EXEC_CG: a process-scoped set of
   (id(executor), id(group), id(node)) triples. trace_executor_add_cbgroup
   short-circuits on repeat triples. See notes/dedup_rclpy_exec.txt
   for design rationale, alternative designs rejected, and an
   audit-health snippet.

5. Second deploy — clean counts
   fish_compose_20260419_161633 (same mission, with dedup):
     aircraft fish_executor_add_cbgroup:  34 events (~3000x reduction)
     aircraft fish_publish_link:          89 events (recovered)
     aircraft fish_executor_init:         26  (13 C++ + 13 rclpy)
     aircraft fish_callback_group_init:   50  (24 C++ + 26 rclpy)
     yolo_py (PID 1252) EX vertex:  SingleThreaded, 1 thread, 1 CG
     9/9 EX vertices in unified graph have executor_type + cb_groups
     (previously 8/9, yolo_py was ?)

6. Paper material
   EXPECTED_OBSERVED.txt now has §7 documenting the rclpy
   add_node/remove_node cycle, the observation-side vs construction-
   side invariant mismatch, and the measured before/after impact.
   dedup_rclpy_exec.txt captures the mechanism in detail.

   The generic lesson for the paper: structurally "the same" API in
   rclcpp vs rclpy can have different invariants when observed at
   runtime. Dedup belongs on the observation side, not baked into
   the tracepoint firing convention.

7. Not yet rclpy-visible
   Multiple rclpy processes showed 1 or 2 exec_init events. Short-
   lived ros2 CLI subprocesses (action send_goal): 1 executor each
   (expected). rviz2: 3 executors (unusual — probably explicit
   application-level executors inside rviz, different from the
   transient spin-helper pattern seen in C++ kiss_icp_node). TBI.


2026-04-18
----------

Big day — extended FISH well beyond where we were after the meeting.
Mission goal from this session: implement/test/update loop for
graph_store persistence, scheduler-introspection tracepoints, and
end-to-end validation on a real AAS mission.

1. graph_store (new persistent graph layer)
   - postprocess/graph_store.py — MongoDB-backed store for the FISH
     graph. Collections in `fish_meta` DB:
       fish_graphs, fish_nodes, fish_edges, fish_mutations (audit log).
   - Public API: save_graph, load_graph, update_node, update_edge,
     add/remove, to_viz_json, export_json, remap_graph, list_graphs.
   - CLI: `python3 graph_store.py list|info|export|delete|mutations`.
   - Round-trip verified: serialize → store → load → export, byte-for-byte
     equivalent on the 108 nodes, 155 edges graph we already had.
   - Docs: docs/graph_store.md + notes/graph_store.txt.

2. model_improved + fish_compose hooked into graph_store
   - model_improved.py main auto-calls save_graph at the end (unless
     --no-db). New flags: --source-trace, --container-role.
   - fish_compose.py fully rewritten: per-container graphs cached as
     `<compose>_<role>`, composed graph as `<compose>` with
     is_composed=True.
   - Boundary matching fixed: cross-container L2 edges are now
     real→real, not real→ext-placeholder. Previously the edge
     targeted an ext entity that we immediately deleted, silently
     losing the cross-container comm. Synthetic 2-container test
     confirmed the fix; the AAS 3-container run produced 30 real→real
     cross-container L2 edges.
   - Flags: --skip-ingest, --from-cache (DB cache only, no extract),
     --force-reextract.

3. Four new scheduler-introspection tracepoints (C++ rclcpp)
   Purpose: answer "what executor type, how many threads, which
   callback groups" directly from trace, not from heuristics.

     fish_executor_init(executor_addr, executor_type, num_threads)
       — in SingleThreadedExecutor / MultiThreadedExecutor /
         StaticSingleThreadedExecutor ctors
     fish_callback_group_init(group_addr, group_type, automatic_add)
       — in CallbackGroup ctor
     fish_cbgroup_add(group_addr, entity_rcl_handle, entity_kind)
       — in add_subscription / add_timer / add_service / add_client /
         add_waitable. Critical detail: entity_addr is the RCL handle
         obtained via `subscription_ptr->get_subscription_handle().get()`
         (and similar for timer / service / client). This lets the
         event JOIN CLEANLY with existing rcl_*_init events, i.e., the
         E vertex is identifiable from the tracepoint payload alone.
         First attempt used the rclcpp wrapper pointer; migration
         logic in install_fish_tracepoints upgrades existing overlays
         in place.
     fish_executor_add_cbgroup(executor_addr, group_addr, node_addr)
       — in Executor::add_callback_group_to_map (covers both explicit
         add_callback_group and the auto-add path through add_node).

   Installers updated (both install.sh and install_fish_tracepoints):
     - tp_call.h / tracetools.h / tracetools.c extended for the 4 new
       tracepoints (declaration + wrapper functions).
     - rclcpp now cloned + copied into the overlay workspace (was
       previously only cloned for rclcpp_action).
     - Build chain: tracetools → rcl → rclcpp → rclcpp_action.
     - install_fish_tracepoints gets a new --scheduler flag, bundled
       into --all / default.
     - names.py (tracetools_trace DEFAULT_EVENTS_ROS) gets +4 entries.
     - fish_events.txt gets +4 entries.

4. Deployed on all 3 AAS containers
   - install_all.sh --cont ran twice (first pass had the wrapper-ptr
     bug; second pass applied the migration to rcl handle).
   - rclcpp rebuilt in overlay — ~5 min per container, parallel, clean.
   - All 3 container images (aircraft-image-fish, simulation-image-fish,
     ground-image-fish) carry the new tracepoints.

5. End-to-end mission: fish_compose_20260418_165627
   - sim_run.sh launched all 3 containers; orchestrator ran takeoff →
     orbit → land (~95 s active flight).
   - Ingest: 13.7M docs in aircraft DB, 2M each for ground/simulation.
   - Model extraction + compose: 129 vertex unified graph (3 CN, 9 EX,
     13 N, ~45 E, ~56 F), 186 edges.
   - Scheduler event yield in aircraft:
       13 fish_executor_init, 24 fish_callback_group_init,
       537 fish_cbgroup_add, 21 fish_executor_add_cbgroup.
   - 32/33 entities ended up with callback_group attribute.
   - 8/9 executors ended up with executor_type + num_threads + cb_groups
     attributes. The 9th (yolo_py) is rclpy — see below.

6. Key findings, paper-relevant
   See notes/EXPECTED_OBSERVED.txt. Summary:
   - Processes have MULTIPLE executors, not one. Two mechanisms:
     transient ST executors from rclcpp::spin() / spin_some() helpers,
     and deliberate persistent parallel executors (e.g., px4_interface
     runs one 20-thread MT + one ST side-by-side).
   - Nodes have MULTIPLE callback groups with MIXED types. AAS aircraft
     has 14 MutuallyExclusive + 23 Reentrant CGs. PX4 /fmu/out/* sensor
     topics specifically use Reentrant so messages can be dispatched
     concurrently on different MT-executor threads.
   - MultiThreadedExecutor default num_threads is
     `std::thread::hardware_concurrency()`, i.e., 20 on this host. Not
     specified anywhere in the AAS source. Same binary on a 4-core
     machine would use 4 threads — scheduling analysis done offline
     from source is structurally blind to this.
   - The canonical ROS 2 scheduling model (1 process = 1 executor,
     1 node = 1 CG) does not match any of the 3 AAS containers.

7. fish_viz.html tweak
   - Tooltip on EX hover: shows "(SingleThreaded)" / "(MultiThreaded,
     20 thr) · N CG".
   - Tooltip on E/F hover: "[MX]" or "[RE]" badge when callback_group
     present.

8. Documentation
   - notes/graph_store.txt (dev reference)
   - notes/EXPECTED_OBSERVED.txt (expected vs observed, paper material)
   - docs/graph_store.md (user-facing)
   - docs/api/postprocess/postprocess.graph_store.rst (Sphinx autodoc)
   - docs/index.md toctree updated
   - notes/immediate_work.txt item 13 (invariant validation — deferred)


rclpy note (don't assume the Python side silently works)
---------------------------------------------------------

The yolo_py process (the only rclpy-based node in this mission)
produced ZERO scheduler events. I concluded "rclpy doesn't have
scheduler tracepoints", which is TRUE for the scheduler-specific
tracepoints (install_rclpy.sh / fish_rclpy_trace.py do NOT yet patch
rclpy/executors.py or its CallbackGroup classes), but the observation
is also muddied by yolo_py's own rough start:

  - PID 712 ran normally: 1 rcl_node_init, 12,374 callback_start
  - GPU daemon killed it to relaunch under nsys → PID 1228
  - PID 1228 never initialized (0 rcl_node_init, 0 callback_start) —
    silent crash during Python/CUDA startup under nsys
  - Eventually recovered as PID 1249: 1 rcl_node_init,
    40,718 callback_start (rclpy subscription / timer / service
    tracepoints were firing normally here)

So the existing rclpy tracepoints (rclpy_*_callback_added,
rclpy_callback_register) ARE working — we have PID-1249 callbacks and
subscription links. What is MISSING is the rclpy-specific
counterparts for the 4 new scheduler tracepoints. Adding them is
mechanically straightforward: patch rclpy/executors.py
SingleThreadedExecutor.__init__, MultiThreadedExecutor.__init__,
MutuallyExclusiveCallbackGroup.__init__, ReentrantCallbackGroup.__init__,
Executor.add_node to call via the existing fish_rclpy_trace ctypes
bridge into the tracetools library.

Also worth calling out for the paper: rclpy's MT executor runs under
the GIL, so concurrency behavior is materially different from rclcpp
MT even with the same CG types. When the rclpy scheduler patch is
added, an additional `lang: "python"` flag on the EX vertex would let
analysis correctly apply GIL constraints.


Next up
-------
  1. Commit (this).
  2. rclpy scheduler tracepoints (above).
  3. Per-callback roofline (immediate_work.txt #9).
  4. Invariant validation on graph mutations (immediate_work.txt #13).
